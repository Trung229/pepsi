import {
    StyleSheet,
} from 'react-native';

const styleContainer = StyleSheet.create({
    container: {
        display: 'flex',
        flex: 1,
        justifyContent: 'center'
    },
    imgBackground:{
        flex: 1,
    }
})

export default styleContainer;